<form method="post" action="{{ path('app_some_m2m_entity_delete', {'id': some_m2m_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ some_m2m_entity.id) }}">
    <button class="btn">Delete</button>
</form>
